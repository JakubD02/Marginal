[project]
name = "marginal"
version = "0.1.0"
description = "Business profitability simulator with AI advisor"
requires-python = ">=3.11"

[project.scripts]
seed = "scripts.seed:seed"
smoke-test = "scripts.smoke_test:main"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]